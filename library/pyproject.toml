[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "flad-flower-strategies"
version = "0.1.0"
description = "Strategies for Flower"
requires-python = ">=3.12"
dependencies = [
  "flwr>=1.29.0"
]

[tool.hatch.build.targets.wheel]
packages = ["strategies"]
